Meeting 1.1 photos go in this folder.

Expected filenames (referenced by content.js):
  01-the-cast-assembles.jpg
  02-room-ran-out-of-couch.jpg

The photo gallery for this meeting is GATED until every student
visible in these photos has a signed Photo Release Form on file.

To make the gallery visible on the site:
  1. Drop the two photo files into this folder using the names above.
  2. Open content.js and find the Meeting 1.1 entry under rov.meetingNotes.
  3. Change `photoConsentPending: true` to `photoConsentPending: false`.
  4. Commit and push.
